Drop the .age files users send you into THIS folder, then double-click
"Open Dashboard.command" in the folder above.

You can sort into subfolders (by month, by org, whatever) — the ingest reads
everything in here recursively and de-duplicates, so re-dropping the same file
is harmless.
